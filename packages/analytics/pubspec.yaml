name: analytics
description: Academia analytics infrastructure subsystem.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.13.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get_it: ^9.2.1
  injectable: ^3.0.0
  posthog_flutter: ^5.12.0

dev_dependencies:
  build_runner: ^2.15.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  injectable_generator: ^3.0.2
