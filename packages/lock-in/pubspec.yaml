name: lock_in
description: Local Android focus-app blocking subsystem for Academia.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  app_blocker:
    path: ../app-blocker
  database:
    path: ../database/
  flutter:
    sdk: flutter
  get_it: ^9.2.1
  injectable: ^3.0.0
  material3_indicators: ^2026.7.12+2

dev_dependencies:
  drift: ^2.34.4
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  build_runner: ^2.16.1
  injectable_generator: ^3.1.3
